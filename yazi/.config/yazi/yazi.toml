#:schema https://yazi-rs.github.io/schemas/yazi.json

[mgr]
show_hidden = true

[[opener.mpv_dir]]
run = 'mpv --autocreate-playlist=filter --shuffle --loop-playlist=inf "$@"'
for = "unix"
orphan = true
desc = "Open in mpv"

[open]
append_rules = [{ mime = "inode/directory", use = "mpv_dir" }]
