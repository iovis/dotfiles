#!/usr/bin/env bash

action=$(printf "suspend\nshutdown\nreboot\nlogout" | vicinae dmenu) || exit 0

case $action in
"logout") loginctl terminate-user david ;;
"reboot") systemctl reboot ;;
"shutdown") systemctl poweroff ;;
"suspend") systemctl suspend ;;
esac
